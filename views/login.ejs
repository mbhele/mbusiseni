<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: white; /* White text */
            background-color: black; /* Black background */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column; /* Vertically align content */
        }

        h1 {
            margin-bottom: 20px;
            color: #0e8a47; /* Green color for heading */
        }

        form {
            background: #333; /* Dark grey background for the form */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.2); /* Stronger shadow for a 3D effect */
            width: 300px; /* Fixed width for better control */
        }
        .home-link {
            position: absolute;
            top: 10px;
            left: 10px;
            color: #0e8a47; /* Adjust color to fit the theme */
        }
        label {
            color: white; /* White labels */
        }

        input[type="text"],
        input[type="password"] {
            width: calc(100% - 16px);
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 20px;
            border: 1px solid #ccc; /* Lighter border */
            border-radius: 4px;
            background-color: #222; /* Darker input backgrounds */
            color: white; /* White input text */
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            border-color: #e70909; /* Red border on focus */
        }

        button {
            background-color: #0e8a47; /* Red background for the button */
            color: white;
            border: none;
            padding: 10px 20px;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0e8a47; /* Green background on hover */
        }

        a {
            color: #e70909; /* Red link */
            text-decoration: none;
        }

        a:hover {
            color: #fff; /* White on hover */
            text-decoration: underline;
        }

        @media (max-width: 600px) {
            form {
                width: 90%; /* Responsive width on smaller screens */
            }
        }
    </style>
</head>
<body>
    <a href="/" class="home-link">&#x2190; Home</a>
    <h1>Login</h1>
    <form action="/login" method="POST">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required><br>

        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required><br>

        <button type="submit">Login</button>
    </form>

    <p>Don't have an account? <a href="/register">Register here</a></p>
</body>
</html>
